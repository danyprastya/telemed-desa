import { LoginForm } from '@/components/auth/LoginForm'

/**
 * Login page: centered card with email + password form.
 * No register link — users are created by admin only.
 */
export default function LoginPage() {
  return <LoginForm />
}
